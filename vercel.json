{
    "version": 2,
    "builds": [
        {
            "src": "/server.js",
            "use": "@now/node",
            "config": { "includeFiles": ["src/**", "pages/**"] }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.js"
        }
    ]
}